[workspace]
members = [".", "rafka-node"]
resolver = "2"

[package]
name = "rafka"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio = { version = "1", features = ["net", "io-util", "rt-multi-thread", "macros", "sync"] }
